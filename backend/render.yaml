services:
  - type: web
    name: trade-gpt-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 5000
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
